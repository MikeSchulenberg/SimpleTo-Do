<!DOCTYPE html>
<html>
    <head>
        <title>Simple To-Do</title>
        <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700" rel="stylesheet">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.2/css/bootstrap.min.css" integrity="sha384-Smlep5jCw/wG7hdkwQ/Z5nLIefveQRIY9nfy6xoR1uRYBtpZgI6339F5dgvm/e9B" crossorigin="anonymous">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand" href="/"><i class="fas fa-tasks"></i>Simple To-Do</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <% if (currentUser) { %>
                        <ul class="navbar-nav ml-auto">
                            <li class="nav-item dropdown bg-primary">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <%= currentUser.username %>
                                </a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenu">
                                    <a class="dropdown-item" href="/logout">Log Out</a>
                                </div>
                            </li>
                        </ul>
                        
                        <div id="user-stats-container">
                            <div id="user-stats" class="d-flex flex-column bd-highlight ml-3">
                                <div class="bd-highlight"><i class="far fa-check-square mr-2"></i>Completed Tasks: <%= currentUser.completedTasks %></div>
                                <div class="bd-highlight"><i class="fas fa-trophy mr-2"></i>Achievement Points: <%= currentUser.achievementPoints %></div>
                            </div>
                        </div>
                        
                        <% } else if (page === "register") { %>
                            <ul class="navbar-nav">
                                <li class="nav-item bg-primary">
                                    <a class="nav-link" href="/login">Log In</a>
                                </li>
                            </ul>
                        <% } else if (page === "login") {%>
                            <ul class="navbar-nav">
                                <li class="nav-item bg-primary">
                                    <a class="nav-link" href="/register">Sign Up</a>
                                </li>
                            </ul>
                    <% } %>
                </div>
            </div>
        </nav>
        
        <% include ../partials/alerts %>